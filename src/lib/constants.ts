/**
 * Shared CLI-style prompt prefix used in screen headers and buttons.
 * Uses non-breaking spaces (\u00a0) for consistent rendering.
 */
export const CLI_PROMPT = ">_\u00a0";

/** Ko-fi donation page — update once account is created. */
export const KOFI_URL = "https://ko-fi.com/PLACEHOLDER";

/** Public GitHub repository. */
export const GITHUB_REPO_URL = "https://github.com/Stratorheus/icebreaker";
